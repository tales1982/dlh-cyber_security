# Initial Email Triage

Rapid first pass over the 8 reported emails, based on headers, authentication results and visible body content only. No link was visited and no attachment was opened. Domains, addresses and IPs are shown in code formatting so they stay exactly as they appear in the headers and are not clickable.

| Email | From | Subject | SPF | DKIM | DMARC | Class | Priority | Evidence |
|---|---|---|---|---|---|---|---|---|
| E1 | `newsletter@healthcare-education-weekly.com` | Your April newsletter: Medication reconciliation best practices | pass | pass | pass | LEGITIMATE | P4-LOW | Fully aligned authentication (mailfrom, DKIM `d=` and From all `healthcare-education-weekly.com`). Bulk newsletter sent through MailChimp with one-click `List-Unsubscribe` and `Precedence: bulk`. Content is educational, links only go to its own domain, no credential request and no urgency. It claims a 2024-08-11 subscription that is not yet verified against the recipient's mailbox history. Graymail, not a threat. |
| E2 | `noreply@meddefense-portal.com` | ACTION REQUIRED: Portal re-verification needed within 24 hours | fail | none | fail | SUSPICIOUS | P1-URGENT | P1 because the batch confirms Diane Marsh clicked this at 2026-04-14 15:02:33 CDT (WS-NURSE-04, `10.10.2.15`), about 15 minutes after delivery. Lookalike domain `meddefense-portal.com` (not `meddefense.com`). SPF fail (`91.234.99.107` not authorized), unsigned, DMARC fail with `action=none`, so the gateway did not block it. PHPMailer 6.6.0, `X-Priority: 1`, 24-hour lockout threat, personalized token in the `/verify/staff` link: credential-harvest lure. |
| E3 | `security@outlook-protection.com` | Unusual sign-in activity detected on your Microsoft 365 account | pass | pass | pass | SUSPICIOUS | P2-HIGH | Impersonates Microsoft (display name `Microsoft Account Protection`) from `outlook-protection.com`, which is not a Microsoft-owned domain. SPF, DKIM and DMARC all pass, but only for the attacker's own domain, so authentication does not make it legitimate. Fake sign-in alert (Lagos, Nigeria), 48-hour lockout threat, `X-Priority: 1`, PHPMailer 6.6.0, link to `outlook-protection.com/verify`: likely credential harvesting. No click reported so far. |
| E4 | `it-announcements@meddefense.com` | Reminder: Quarterly password change window opens April 20 | pass | pass | pass | LEGITIMATE | P4-LOW | Internal: relayed from `exchange-hub.meddefense.local` (`10.10.1.15`), SPF, DKIM and DMARC pass for `meddefense.com`, Exchange 2019, `List-ID` distribution list. Plain-text reminder with no hyperlinks, no attachment and no credential request. It states that IT never emails password-change links, which matches policy and contradicts the E2 pretext. |
| E5 | `invoices@medequip-supplies.net` | Invoice INV-2026-04891 - Payment required within 7 days | softfail | none | fail | SUSPICIOUS | P2-HIGH | Vendor-invoice lure from `medequip-supplies.net`: SPF softfail (`185.176.43.22`), unsigned, DMARC fail; PHPMailer 6.6.0, `X-Priority: 1`. USD 24,716.38 due in 7 days with a late-fee threat, generic greeting, two links to `medequip-supplies.net` (`/invoices/pay` and `/portal/login`) and a PDF attachment (not opened). The Accounts Payable recipient already reports the invoice looks wrong and the vendor is unverified: possible payment fraud plus credential harvesting. |
| E6 | `deals@canadian-pharma-discount.org` | 90% OFF Viagra, Cialis, Xanax - No prescription needed!!! | softfail | none | fail | SPAM | P4-LOW | Unsolicited pharmacy advertising sent with `XPedia Bulk Mailer 4.2`. SPF softfail, unsigned, DMARC fail (`action=quarantine`), `X-Spam-Score: 9.8` against a 5.0 threshold with tests `DRUGS_ERECTILE`, `NORMAL_HTTP_TO_IP` and `NUMERIC_HTTP_ADDR`. The link goes to a raw IP (`203.0.113.228`, the same as the sending host). No MedDefense impersonation, no credential request, no attachment: noise, not targeted. |
| E7 | `hr-notifications@meddefense-benefits.org` | Open Enrollment closes TOMORROW - action required | fail | none | fail | SUSPICIOUS | P2-HIGH | Lookalike of our own brand: `meddefense-benefits.org` (not `meddefense.com`). SPF fail (`164.90.218.73` not authorized), unsigned, DMARC fail; PHPMailer 6.6.0, `X-Priority: 1`. "Closes tomorrow" deadline with a coverage-lapse threat and a link to `meddefense-benefits.org/enroll`. The recipient (Billing) reports she never signed up for anything. HR-themed, role-targeted lure. |
| E8 | `HC3@hhs.gov` | [HC3 ALERT - TLP:CLEAR] Active phishing campaign targeting regional healthcare | pass | pass | pass | LEGITIMATE | P3-MEDIUM | Sector alert from `hhs.gov`: SPF, DKIM and DMARC pass for the same domain, sender IP `134.174.47.82` authorized, TLS 1.3, plain text marked TLP:CLEAR. No links to act on, no attachment, no credential request, no deadline pressure. Not a threat; P3 only because it drives follow-up (IOC submission to HC3). Its traits (lookalike domains, PHPMailer sending, 24-48 hour urgency, role-targeted lures) match E2, E3, E5 and E7. |

## Triage Summary

- SPAM: 1 (E6)
- SUSPICIOUS: 4 (E2, E3, E5, E7)
- LEGITIMATE: 3 (E1, E4, E8)
- Highest priority: E2 (P1-URGENT) - confirmed click by Diane Marsh on WS-NURSE-04, possible credential compromise
