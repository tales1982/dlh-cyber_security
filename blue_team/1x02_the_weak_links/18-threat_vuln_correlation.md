# 18. The Threat-Vulnerability Correlation — MedDefense Vulnerability Scan

**Analyst:** Threat Intelligence Analyst
**Date:** Current

## Threat-Vulnerability Correlation Matrix

| Finding | Threat Actor(s) (1x01 T6) | Vector (1x01) | Kill Chain (1x01 T10) | Scenario (1x01 T14) | Gap (1x00) |
|---|---|---|---|---|---|
| **031** (Ghostcat, `ehr-srv-01`) | Ransomware Groups (primary target: `ehr-db-01`); Unskilled/Opportunistic (trivial public exploit) | Not a first-touch vector — reached only after initial access via any other vector (phishing, credential purchase) | **None named explicitly** — a documentation gap; structurally matches Kill Chain #2 Step 3's pattern | Partially implied by Scenario 1 ("Operation Flatline") Step 5 exfiltration from `ehr-db-01`, but Ghostcat itself is never named as the mechanism | GAP-001, GAP-002 |
| **003** (PostgreSQL open, `ehr-db-01`) | Ransomware Groups (explicit primary target, T6); Insider (Malicious, T6); External attacker via compromised vendor (Kill Chain #5's own actor) | Purchased/harvested valid credentials (22% of healthcare initial access, T0); vendor access pathway | **Kill Chain #5, Step 3** — named directly by Gap ID ("GAP-001 — ehr-db-01 is reachable network-wide, not restricted to ehr-srv-01 alone") | **Scenario 3, "The MedTech Backdoor," Step 3** — named explicitly, word for word: "the session reaches ehr-db-01 directly, since the database is not restricted to ehr-srv-01 alone" | GAP-001 |
| **001/002** (Apache RCE chain, `billing-srv-01`) | Unskilled / Opportunistic Attacker — explicit and proven, not projected ("already compromised MedDefense twice," T6) | Exploitation of known, unpatched, internet-facing vulnerabilities | **Kill Chain #4, Step 1** — named directly | **None** — none of the three T14 scenarios starts here, a notable gap given this is the only pattern with confirmed real-world repetition | GAP-008, GAP-005 |
| **004** (Windows XP, `WS-RAD-01`) | Unskilled / Opportunistic Attacker (wormable, self-propagating exploits match this actor's profile) | Automated exploitation of known, unpatched vulnerabilities (no direct T6 citation of this specific host) | **None named** — a documentation gap flagged repeatedly across this project | **None** | GAP-007, GAP-012 |
| **015** (NAS-01 exposure) | Ransomware Groups — explicit actor for both citing deliverables below | Follows an initial phishing/credential vector into the chain; this finding is the objective-execution step, not the entry point | **Kill Chain #1, Step 6** — named directly | **Scenario 1, "Operation Flatline," Step 6** — named explicitly: "NAS-01 backups and Volume Shadow Copies are deleted" | GAP-003 |
| **016** (Philips HTTP interfaces) | Unskilled / Opportunistic Attacker (Kill Chain #4's actor, generalized to the fleet) | Flat-network lateral movement following any initial foothold | **Kill Chain #4, Steps 3-4** — the medical IoT fleet category is named, though Philips specifically (vs. BD Alaris) is not singled out | **None** — no T14 scenario touches any medical device, a real coverage gap given this category's Critical asset rating | GAP-007 |
| **007** (LDAP/SMBv1, `ad-dc-01`) | Ransomware Groups — T6 explicitly cites "GAP-017 (no AD tiering) — domain-wide escalation" as this actor's exposure | A distinct, independent technique (LDAP relay) reaching the same outcome (Domain Admin) as the documented pass-the-hash path | **Kill Chain #1, Step 4** — same host, different specific technique (credential dumping/pass-the-hash rather than LDAP relay) | **Scenario 1, Step 4** — named explicitly: "Mimikatz dumps a cached Domain Admin hash; pass-the-hash grants Domain Admin on ad-dc-01" | GAP-017, GAP-002 |
| **029** (Grafana, Westside shadow IT) | Unskilled / Opportunistic Attacker (most plausible, though the device's actual internet exposure path is itself unconfirmed) | No named T6 vector fits cleanly — this is a genuine edge case outside the documented vector taxonomy | **None** | **None** | GAP-011 |

## Which Single Vulnerability Would Cause the Most Damage?

**Finding 003 — PostgreSQL Unrestricted Network Access on `ehr-db-01`.** The tie-breaker is not asset criticality alone (Findings 031, 015 and 016 are equally tied to Critical assets) — it is the **number of independent threat paths that converge on this exact gap**. Finding 003 is the only vulnerability in this matrix named explicitly, by two different threat actor types, in two separate 1x01 deliverables: Kill Chain #5 describes it as the mechanism a compromised third-party vendor session would abuse, and Scenario 3 independently arrives at the identical technical detail — "the database is not restricted to `ehr-srv-01` alone" — for a completely different attacker profile (an external attacker via MedTech Solutions rather than an internal ransomware affiliate). When a single unremediated gap is the common denominator across multiple, structurally different attack narratives written independently in an earlier project, that is a much stronger signal of real-world inevitability than a single kill chain citation. Combine that with the fact that exploiting it requires no exploit code at all — only network reachability, which the flat topology already guarantees to every one of MedDefense's ~350+ devices — and Finding 003 represents the shortest, most actor-agnostic, most probability-weighted path to the single worst outcome in the entire environment: full compromise of the patient database that anchors every one of MedDefense's Top-5 Critical Assets.

## Repo

- **GitHub repository:** `dlh-cyber_security`
- **Directory:** `blue_team/1x02_the_weak_links`
- **File:** `18-threat_vuln_correlation.md`
